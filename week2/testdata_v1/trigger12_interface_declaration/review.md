# gosu-review 리뷰 리포트

## 요약
- 검토 파일: 1개
- 새로 발견된 문제: 0건 (변경 라인)
- 기존 문제: 2건 (참고)

---

## fixture.go

### 🟡 기존 라인의 문제 (참고)

#### L3 `type Notifier interface`
**[Rule 5] 인터페이스 선제 생성 금지**
- 근거: 해당 파일에서 인터페이스(`Notifier`)를 구현하는 구조체가 `EmailNotifier` 단 1개뿐입니다. 향후 다형성이 명확하게 요구되기 전에 미리 인터페이스를 만드는 것은 불필요한 추상화일 수 있습니다. (프로젝트 차원의 검토가 필요합니다)
- 제안: 테스트나 다형성 목적이 명확하지 않다면 인터페이스를 제거하고 구체 타입을 직접 사용하세요.

#### L3 `type Notifier interface`
**[Rule 6] 인터페이스 위치 (클라이언트 측 정의)**
- 근거: 인터페이스가 그것을 구현하는 구체 타입(`EmailNotifier`)과 같은 패키지에 정의되어 있습니다. Go에서는 인터페이스를 이를 사용하는 클라이언트 패키지에 정의하는 것이 결합도를 낮추는 권장 패턴입니다.
- 제안: 이 인터페이스를 실제로 사용하는 패키지로 이동시키세요.
