# gosu-review 리뷰 리포트

## 요약
- 검토 파일: 1개 / 새 위반: 7건 / 기존 위반: 0건

---

## fixture.go

### 🔴 변경 라인
- **L19** `NewParcel` — [Rule 7] 인터페이스 반환 지양
  - 근거: 출력으로 인터페이스(`Shippable`)를 반환하여 호출 측이 구체 타입의 이점을 누릴 수 없습니다.
  - 제안: 구체 타입인 `Parcel`을 반환하도록 시그니처를 변경합니다.
- **L23** `Lookup` — [Rule 8] any 남용
  - 근거: 반환 타입으로 `any`를 사용하여 컴파일 타임의 타입 안정성을 잃었습니다.
  - 제안: 제네릭 또는 명확한 구체 타입을 반환하도록 시그니처를 수정합니다.
- **L34** `FindOrder` — [Rule 44] nil 인터페이스 반환 함정
  - 근거: 구체 포인터 `*MyError`가 nil일 때 반환하면 호출 측에서 `err != nil`이 참이 됩니다.
  - 제안: `if e == nil { return nil }` 처리를 추가하여 명시적으로 nil을 반환합니다.
- **L42** `LoadConfig` — [Rule 45] io.Reader 활용
  - 근거: 파일 이름(`string`)을 받아 내부에서 `os.Open`을 호출하고 있어 테스트와 재사용이 어렵습니다.
  - 제안: `io.Reader`를 입력받도록 시그니처를 변경합니다.
- **L58** `NewClient` — [Rule 11] 함수형 옵션 패턴
  - 근거: 파라미터가 6개로 많고 확장에 취약합니다.
  - 제안: 필수 파라미터 외에는 옵션 패턴(`opts ...Option`)을 도입합니다.
- **L64** `Address` — [Rule 42] 리시버 타입 결정 (값 vs 포인터)
  - 근거: 같은 `Client` 타입에 대해 `Address`는 값 리시버, `SetHost`는 포인터 리시버를 혼용하고 있습니다.
  - 제안: 일관성을 위해 모두 포인터 리시버 `*Client`로 통일합니다.
- **L67** `Split` — [Rule 43] 기명 반환값
  - 근거: 단순한 함수에서 불필요하게 기명 반환값(`first, rest string`)을 사용하여 가독성을 저하시킬 수 있습니다.
  - 제안: 익명 반환값 `(string, string)`을 사용하고 명시적으로 반환합니다.
