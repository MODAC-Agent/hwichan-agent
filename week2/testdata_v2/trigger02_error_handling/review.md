# gosu-review 리뷰 리포트

## 요약
- 검토 파일: 1개 / 새 위반: 2건 / 기존 위반: 0건

---

## fixture.go

### 🔴 변경 라인
- **L18** `LoadUser` — [Rule 51] 에러 이중 처리
  - 근거: 에러 발생 시 `log.Printf`로 로깅한 후 동일한 에러를 다시 반환하고 있습니다.
  - 제안: 둘 중 하나만 선택하여 계층 경계에서 로깅하거나 래핑해서 반환합니다.
- **L27** `LoadUserWrapped` — [Rule 48] 에러 래핑 전략 (`%w` vs `%v`)
  - 근거: 경계 에러 래핑에 `%v`를 사용하여 호출 측에서 `errors.Is`/`errors.As`를 통한 에러 판별이 불가능해집니다.
  - 제안: `fmt.Errorf` 내 동사를 `%w`로 변경하여 원본 에러를 보존합니다.
