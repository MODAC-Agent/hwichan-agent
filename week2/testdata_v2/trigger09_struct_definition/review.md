# gosu-review 리뷰 리포트

## 요약
- 검토 파일: 1개 / 새 위반: 2건 / 기존 위반: 0건

---

## fixture.go

### 🔴 변경 라인
- **L7** `GetName`, `SetName` — [Rule 4] 게터/세터 기계적 적용
  - 근거: 검증 로직 없이 단순 필드 접근을 위해 관용적이지 않은 `Get`/`Set` 접두사를 붙인 메서드를 사용했습니다.
  - 제안: 필드를 직접 노출(`Name string`)하거나 접두사 없는 메서드(`Name() string`)를 사용합니다.
- **L11** `Cache` — [Rule 10] 임베딩 가시성
  - 근거: `sync.Mutex`를 익명 필드(임베딩)로 선언하여 `Lock()`과 `Unlock()` 메서드가 구조체 외부에 그대로 노출됩니다.
  - 제안: 명명된 필드 `mu sync.Mutex`로 변경하여 캡슐화를 유지합니다.
