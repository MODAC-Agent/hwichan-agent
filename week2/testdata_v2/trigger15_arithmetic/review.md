# gosu-review 리뷰 리포트

## 요약
- 검토 파일: 1개 / 새 위반: 3건 / 기존 위반: 0건

---

## fixture.go

### 🔴 변경 라인
- **L5** `ParseAge` — [Rule 18] 정수 오버/언더플로
  - 근거: `strconv.Atoi`로 받은 정수(플랫폼에 따라 `int64`)를 `int32`로 좁혀 형변환하면서 범위 초과를 검사하지 않습니다.
  - 제안: 형변환 전 값이 `math.MaxInt32` 이하, `math.MinInt32` 이상인지 가드 조건으로 확인하세요.
- **L10** `IsExpectedPrice` — [Rule 19] 부동소수점 직접 비교 지양
  - 근거: `float64` 변수인 `price`와 `expected`를 `==` 연산자로 직접 비교하여 부동소수점 오차로 인해 잘못된 결과가 나올 수 있습니다.
  - 제안: `math.Abs(price - expected) < 1e-9`와 같이 엡실론 범위를 이용한 비교로 대체하세요.
- **L14** `AccumulateSmall` — [Rule 20] 부동소수점 연산 순서
  - 근거: 큰 부동소수점 값에 작은 값을 누적 합산하면 정밀도 한계로 인해 작은 값들이 손실될 수 있습니다.
  - 제안: 작은 값들(`tinies`)을 먼저 합산한 뒤 마지막에 큰 값(`bigVal`)을 더하는 순서로 연산하세요.
